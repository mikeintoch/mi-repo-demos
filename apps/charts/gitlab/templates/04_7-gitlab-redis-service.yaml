---
apiVersion: v1
kind: Service
metadata:
  name: gitlab-redis
  namespace: {{ .Values.gitlab.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  selector:
    app: gitlab-redis
  ports:
  - port: 6379
    targetPort: 6379
  type: ClusterIP
...